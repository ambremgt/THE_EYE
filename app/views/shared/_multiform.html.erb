<!-- MultiStep Form -->
<div class="row d-flex m-0 align-items-center">
  <!-- LEFT SIDEBAR -->
  <div class="col leftform align-items-center">

    <div  class="blocktoPic">
      <h2 class="fs-title animated fadeIn">Create your shotlist</h2>
        <%= render partial: 'shotlist_text' %>
    </div>

    <div class="blocktoPic d-none">
      <h2 class="fs-title">Create your shotlist</h2>
      <%= render partial: 'shotlist_text' %>
    </div>

    <div class="blocktoPic d-none">
      <h2 class="fs-title">Create your shotlist</h2>
      <%= render partial: 'shotlist_text' %>
    </div>

  </div>
  <!-- end LEFT SIDEBAR -->

  <!-- FORM CENTER -->

  <div class="col-md-8 col-md-offset-3 align-items-center">
    <div class="multiformcustom align-items-center">
        <!-- progressbar -->
        <div class="multiform-progress">
          <ul id="progressbar">
            <li class="active">Title</li>
            <li>Description</li>
            <li>Tags</li>
          </ul>
          <hr style="background-color: lightgrey">
        </div>

        <%= simple_form_for(@shotlist) do |f| %>
        <div class="multiform-content">
          <%= f.error_notification %>
          <!-- fieldsets -->
          <fieldset class="blocktoIt">
            <h1 class="fs-subtitle">Give your film a title</h1>
            <%= f.input :title,label: false, placeholder: "Wayne's World seems a cool title, isn't it?", class: "inp" %>
          </fieldset>

          <fieldset class="blocktoIt d-none">
            <h1 class="fs-subtitle">What is your film about?</h1>
            <%= f.input :description, label: false, placeholder: "It's party time excellent", class: "inp" %>
          </fieldset>

          <fieldset class="blocktoIt d-none">
            <h1 class="fs-subtitle">Select tag that correspond to your project:</h1>

            <fieldset class="form-group check_boxes optional shotlist_shotlist_tag_list">
              <div class="category-choices">
                <input type="hidden" name="shotlist[shotlist_tag_list][]" value="">
                <div class="category-choices-grid">
                  <% @tags.each do |tag| %>
                    <div class="form-check">
                      <input class="form-check-input check_boxes optional" type="checkbox" value="<%= tag %>" name="shotlist[shotlist_tag_list][]" id="shotlist_shotlist_tag_list_<%= tag %>" hidden >
                      <label class="category-choice form-check-label collection_check_boxes" for="shotlist_shotlist_tag_list_<%= tag %>"><%= tag %></label>
                    </div>

                  <% end %>
                </div>
              </div>
            </fieldset>
          </fieldset>
        </div>

        <div class="multiform-bottom">
          <hr style="background-color: lightgrey">

          <button type="button" id="prev" class="d-none btn btn-navbar2-transparent btn-lg" >Previous</button>
          <button type="button" id="next" class="btn btn-navbar2">Next</button>
          <%= f.button :submit ,"Next", id: "sub", class: "submit btn btn-navbar2 d-none"%>
        </div>

      <% end %>

      <!-- link to designify.me code snippets -->
      <!-- /.link to designify.me code snippets -->
    </div>
  </div>
  <!-- end FORM CENTER -->
</div>

<!-- /.MultiStep Form -->


